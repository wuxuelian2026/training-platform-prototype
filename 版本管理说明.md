# 原型 HTML 版本管理

## 目录

- `培训后台.html`：根目录快捷打开文件，跳转到 `current/admin/index.html`。
- `current/`：当前开发和评审版本。
- `releases/V1.0.0/`：已冻结发布版本，只读保存。
- `changes/`：原型变更单。

## 规则

1. 当前开发修改只进入 `current/`，完成评审后复制整个三端工程目录到新的发布版本。
2. 根目录 `培训后台.html` 仅作为快捷预览入口，不作为历史版本保存位置。
3. `current/index.html` 是 Vite 工程标准入口，`current/培训后台.html` 是中文兼容入口；`current/admin/`、`current/teacher/`、`current/learner/` 分别对应后台、教师端和学员端；`current/shared/` 只存放三端共享资源。
4. 已发布版本不得直接覆盖；页面或交互变化使用 `UI-YYYY-XXX` 变更单。
5. 影响状态、字段、权限或业务流程的原型变更，必须同步更新开发实施 PRD 和需求追踪矩阵。
6. 视觉微调使用修订版本，例如 `V1.0.1`；新增功能使用次版本，例如 `V1.1.0`；破坏既有交互的变更使用主版本，例如 `V2.0.0`。
