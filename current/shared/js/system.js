const systemRoot = document.querySelector('[data-system-page]');
const systemPage = systemRoot?.dataset.systemPage;
let systemData = null;
let systemToastTimer;

const escapeHtml = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
const statusClass = (value) => ({ 启用: 'green', 禁用: 'gray', 草稿: 'gray', 已保存: 'green', 有效: 'green' }[value] || 'gray');
const tag = (value) => `<span class="tag ${statusClass(value)}">${escapeHtml(value)}</span>`;
function showToast(message) { const element = document.querySelector('[data-system-toast]'); if (!element) return; element.textContent = message; element.hidden = false; clearTimeout(systemToastTimer); systemToastTimer = setTimeout(() => { element.hidden = true; }, 2800); }
function closeDialog() { document.querySelector('[data-system-dialog]')?.remove(); }
function openDialog(title, subtitle, body, actions = '<button type="button" class="button" data-dialog-close>关闭</button>') { closeDialog(); const dialog = document.createElement('dialog'); dialog.className = 'system-dialog'; dialog.dataset.systemDialog = 'true'; dialog.innerHTML = `<div class="system-dialog-card"><div class="system-dialog-header"><div><h2>${escapeHtml(title)}</h2><p>${escapeHtml(subtitle)}</p></div><button type="button" class="icon-button" data-dialog-close title="关闭" aria-label="关闭">×</button></div>${body}<div class="system-dialog-actions">${actions}</div></div>`; document.body.append(dialog); dialog.showModal(); return dialog; }
function pageFrame(title, description, controls, content) { if (!systemRoot) return; systemRoot.innerHTML = `<div class="system-page"><div class="page-head"><div><h1>${title}</h1><p>${description}</p></div>${controls || ''}</div>${content}<div class="toast" data-system-toast role="status" aria-live="polite" hidden></div></div>`; }
function field(label, name, type = 'text', placeholder = '', wide = false, value = '') { return `<label class="form-field${wide ? ' wide' : ''}"><span>${label}</span><input type="${type}" name="${name}" placeholder="${placeholder}" value="${escapeHtml(value)}"></label>`; }
function select(label, name, options, wide = false, all = true) { return `<label class="form-field${wide ? ' wide' : ''}"><span>${label}</span><select name="${name}">${all ? `<option value="">全部${label}</option>` : ''}${options.map((item) => `<option value="${escapeHtml(item)}">${escapeHtml(item)}</option>`).join('')}</select></label>`; }
function filterPanel(id, fields) { return `<div class="filter-panel" data-filter-drawer-shell="${id}"><button type="button" class="button filter-drawer-trigger" data-filter-drawer-trigger="${id}" aria-expanded="false" aria-controls="${id}"><span class="filter-trigger-icon" aria-hidden="true"></span>筛选条件</button><div class="filter-drawer-backdrop" data-filter-drawer-backdrop="${id}" hidden></div><form class="card filter-form" id="${id}"><div class="filter-head"><strong>筛选条件</strong><span>当前仅展示原型数据</span><button type="button" class="icon-button filter-drawer-close" data-filter-drawer-close="${id}" title="关闭筛选条件" aria-label="关闭筛选条件">×</button></div><div class="filter-grid system-filter-grid">${fields}</div><div class="filter-actions"><button type="reset" class="button">重置</button><button type="submit" class="button primary">查询</button></div></form></div>`; }
function table(head) { return `<div class="table-wrap system-table"><table>${head}<tbody class="table-body"></tbody></table></div><div class="pagination-bar"><span class="result-count">—</span><button class="button" disabled>上一页</button><button class="button primary">1</button><button class="button" disabled>下一页</button></div>`; }
function metrics(items) { return `<div class="card-grid compact-metrics">${items.map(([label, value, note]) => `<div class="card"><div class="metric-label">${label}</div><div class="metric-value">${value}</div><div class="metric-note">${note}</div></div>`).join('')}</div>`; }
function renderRows(rows, template) { const body = systemRoot.querySelector('.table-body'); if (!body) return; body.innerHTML = rows.length ? rows.map((row) => `<tr data-row-id="${escapeHtml(row.id)}">${template(row)}</tr>`).join('') : '<tr><td colspan="20"><div class="empty">暂无符合条件的数据。</div></td></tr>'; const count = systemRoot.querySelector('.result-count'); if (count) count.textContent = `共 ${rows.length} 条`; }
function bindFilter(id, source, predicate, template) { document.querySelector(`#${id}`)?.addEventListener('submit', (event) => { event.preventDefault(); renderRows(source.filter(predicate(event.currentTarget)), template); }); document.querySelector(`#${id}`)?.addEventListener('reset', () => setTimeout(() => renderRows(source, template), 0)); }

const users = [
  { id: 'user-li', name: '李教务', account: 'jw_admin', phone: '138****2026', role: '教务主管', status: '启用', lastLogin: '2026-09-08 08:42', ip: '10.0.1.26' },
  { id: 'user-zhou', name: '周教研', account: 'research_01', phone: '139****3098', role: '教研主管', status: '启用', lastLogin: '2026-09-07 17:21', ip: '10.0.1.18' },
  { id: 'user-chen', name: '陈财务', account: 'finance_01', phone: '137****6812', role: '财务', status: '启用', lastLogin: '2026-09-07 15:08', ip: '10.0.1.33' },
  { id: 'user-old', name: '赵运营', account: 'operator_old', phone: '136****0048', role: '课程顾问', status: '禁用', lastLogin: '—', ip: '—' }
];
const roles = [
  { id: 'role-super', name: '超级管理员', key: 'super_admin', description: '全局配置、系统运维', users: 2, modules: '全部模块', updated: '2026-09-01', status: '启用', preset: true },
  { id: 'role-research', name: '教研主管', key: 'research_lead', description: '课程质量、教师资质审核', users: 2, modules: '课程中心、师资证书审核', updated: '2026-09-06', status: '启用', preset: true },
  { id: 'role-academic', name: '教务主管', key: 'academic_lead', description: '教学调度、教务执行、物资管理', users: 3, modules: '教务、招生、师资、物资', updated: '2026-09-08', status: '启用', preset: true },
  { id: 'role-consultant', name: '课程顾问', key: 'course_consultant', description: '客户跟进、销售转化', users: 6, modules: '面授招生与CRM', updated: '2026-09-02', status: '启用', preset: true },
  { id: 'role-finance', name: '财务', key: 'finance', description: '财务核算、对账', users: 2, modules: '财务中心、商城订单', updated: '2026-09-04', status: '启用', preset: true }
];
const permissionGroups = [
  ['工作台', ['查看本人待办']], ['师资中心', ['查看', '档案维护', '证书审核']], ['课程中心', ['查看', '课程申报审核', '内容与资源管理']], ['商城运营', ['查看订单', '收款记录', '订单处理']], ['面授招生与CRM', ['线索与试听', '报名转化', '班级与订单处理']], ['物资中心', ['查看', '维护', '出入库', '台账导出']], ['教务执行监管', ['查看', '排班与执行', '结业复核']], ['财务中心', ['工资管理', '收款记录', '退款处理']], ['系统管理', ['用户管理', '角色权限', '参数配置']]
];
const settings = { platform: '湖北艺术职业学院继续教育', timezone: 'Asia/Shanghai（UTC+8）', defaultLessonMinutes: 90, homeworkDeadlineHours: 72, attendanceThreshold: 80, homeworkThreshold: 80, switches: { teacherApplication: true, anonymousConsultation: true, messageRetry: true, consultantTrial: true } };
const studentUsers = [
  { id: 'parent-001', accountNo: 'STU-20260001', name: '演示家长A', phone: '138****2026', login: '验证码登录', status: '启用', registered: '2026-08-20 09:12', firstLogin: '2026-08-20 09:13', lastLogin: '2026-09-11 10:20', ip: '10.0.***.26', students: [{ name: '林知夏', relation: '女儿', gender: '女', age: '9岁', phone: '138****1122', status: '正常', current: true, classes: 1, courses: 2 }, { name: '林知远', relation: '儿子', gender: '男', age: '10岁', phone: '138****3344', status: '正常', current: false, classes: 0, courses: 1 }], audits: [] },
  { id: 'parent-002', accountNo: 'STU-20260002', name: '演示家长B', phone: '139****2027', login: '微信授权', status: '启用', registered: '2026-08-22 14:05', firstLogin: '2026-08-22 14:06', lastLogin: '2026-09-10 16:40', ip: '10.0.***.27', students: [{ name: '周予安', relation: '女儿', gender: '女', age: '10岁', phone: '139****7788', status: '正常', current: true, classes: 1, courses: 1 }], audits: [] },
  { id: 'parent-003', accountNo: 'STU-20260003', name: '历史演示账号', phone: '136****0048', login: '密码登录', status: '禁用', registered: '2026-07-15 11:20', firstLogin: '2026-07-15 11:22', lastLogin: '2026-08-30 18:01', ip: '10.0.***.48', disabledReason: '用户主动申请停用', students: [{ name: '赵一诺', relation: '儿子', gender: '男', age: '8岁', phone: '136****9911', status: '正常', current: true, classes: 0, courses: 0 }], audits: [{ action: '禁用', before: '启用', after: '禁用', operator: '超级管理员', time: '2026-08-30 18:05', reason: '用户主动申请停用' }] }
];
const studentUserRole = new URLSearchParams(window.location.search).get('role') || systemRoot?.closest('[data-admin-shell]')?.dataset.role || 'super_admin';
const studentUserAccounts = studentUsers.map((item) => ({ ...item, avatar: item.name.slice(-1), loginMethod: item.login, registeredAt: item.registered, firstLoginAt: item.firstLogin, lastLoginAt: item.lastLogin, lastLoginIp: item.ip, consentAt: item.registered, stateChangedAt: item.audits[0]?.time || item.registered, stateChangedBy: item.audits[0]?.operator || '系统', learners: item.students.map((student) => ({ ...student, birthday: student.age, linkedAt: item.registered })), audits: item.audits.map((audit) => ({ ...audit, at: audit.at || audit.time })) }));

function renderStudentUsers() { const rows = studentUsers; const role = new URLSearchParams(location.search).get('role') || 'super_admin'; if (['research_lead', 'course_consultant', 'finance'].includes(role)) { pageFrame('无权限访问', '当前角色未配置学员用户管理权限。', '', '<div class="card empty">暂无权限查看该页面，请联系超级管理员。</div>'); return; } const canToggle = role === 'super_admin'; pageFrame('学员用户管理', canToggle ? '超级管理员可查看并启停账号；关联学员只读展示，敏感信息默认脱敏。' : '教务主管仅可查看账号和关联学员，不能执行启停操作。', '', metrics([['账号总数', rows.length, '家长/监护人账号'], ['启用', rows.filter(r => r.status === '启用').length, '可正常登录'], ['禁用', rows.filter(r => r.status === '禁用').length, '登录已阻断'], ['关联学员', rows.reduce((n, r) => n + r.students.length, 0), '详情完整展示']]) + filterPanel('student-user-filter', field('关键词', 'keyword', 'text', '昵称 / 手机号 / 账号编号', true) + select('账号状态', 'status', ['启用', '禁用']) + select('登录方式', 'login', ['验证码登录', '密码登录', '微信授权'])) + table('<thead><tr><th>账号</th><th>登录手机号</th><th>登录方式</th><th>关联学员数</th><th>状态</th><th>最近登录</th><th>注册时间</th><th>操作</th></tr></thead>')); const row = r => `<td><strong>${r.name}</strong><br><span class="muted">${r.accountNo}</span></td><td>${r.phone}</td><td>${r.login}</td><td>${r.students.length}</td><td>${tag(r.status)}</td><td>${r.lastLogin}</td><td>${r.registered}</td><td class="action-cell"><button class="text-button" data-system-action="student-detail">查看详情</button>${canToggle ? `<button class="text-button ${r.status === '启用' ? 'danger-link' : ''}" data-system-action="student-toggle">${r.status === '启用' ? '禁用' : '启用'}</button>` : ''}</td>`; renderRows(rows, row); bindFilter('student-user-filter', rows, form => { const keyword = form.keyword.value.trim(); return r => (!keyword || `${r.name}${r.phone}${r.accountNo}`.includes(keyword)) && (!form.status.value || r.status === form.status.value) && (!form.login.value || r.login === form.login.value); }, row); }
function openStudentDetail(row) { const students = row.students.map(s => `<tr><td>${s.name}</td><td>${s.relation}</td><td>${s.gender}</td><td>${s.age}</td><td>${s.phone}</td><td>${s.status}</td><td>${s.current ? '是' : '否'}</td><td>${s.classes}</td><td>${s.courses}</td></tr>`).join(''); const audits = row.audits.length ? row.audits.map(a => `<tr><td>${a.action}</td><td>${a.before}</td><td>${a.after}</td><td>${a.operator}</td><td>${a.time}</td><td>${a.reason}</td></tr>`).join('') : '<tr><td colspan="6">暂无状态变更记录</td></tr>'; openDialog(`${row.name} · 学员用户详情`, `${row.accountNo} · 账号与关联学员均为演示数据`, `<h3>账号信息</h3><dl class="system-detail-list"><dt>登录手机号</dt><dd>${row.phone}</dd><dt>登录方式</dt><dd>${row.login}</dd><dt>状态</dt><dd>${tag(row.status)}</dd><dt>注册时间</dt><dd>${row.registered}</dd><dt>首次登录</dt><dd>${row.firstLogin}</dd><dt>最近登录/IP</dt><dd>${row.lastLogin} / ${row.ip}</dd><dt>禁用原因</dt><dd>${row.disabledReason || '—'}</dd></dl><h3>关联学员</h3><div class="table-wrap"><table><thead><tr><th>姓名</th><th>关系</th><th>性别</th><th>年龄段</th><th>手机号</th><th>状态</th><th>当前</th><th>在读班级</th><th>已报名课程</th></tr></thead><tbody>${students}</tbody></table></div><h3>操作记录</h3><div class="table-wrap"><table><thead><tr><th>操作</th><th>操作前</th><th>操作后</th><th>操作人</th><th>时间</th><th>原因</th></tr></thead><tbody>${audits}</tbody></table></div>`); }
function renderUsers() {
  systemData = users;
  pageFrame('后台用户管理', '管理后台账号、角色和登录状态；禁用后用户无法登录，历史业务数据保留。', '<button class="button primary" data-system-action="user-create">新增用户</button>', metrics([['用户总数', users.length, '后台账号'], ['启用', users.filter((u) => u.status === '启用').length, '可正常登录'], ['禁用', users.filter((u) => u.status === '禁用').length, '账号不可登录'], ['今日登录', users.filter((u) => u.lastLogin.startsWith('2026-09-08')).length, '按最后登录时间']]) + filterPanel('user-filter', select('用户状态', 'status', ['启用', '禁用']) + select('所属角色', 'role', ['超级管理员', '教研主管', '教务主管', '课程顾问', '财务']) + field('关键词', 'keyword', 'text', '姓名 / 账号 / 手机号', true)) + '<p class="system-note">用户列表按最后登录时间降序排列，从未登录账号置于末尾；涉及登录状态变化和密码重置的操作均需要二次确认。</p>' + table('<thead><tr><th>用户</th><th>登录账号</th><th>手机号</th><th>所属角色</th><th>状态</th><th>最后登录</th><th>最后登录 IP</th><th>操作</th></tr></thead>'));
  const row = (item) => `<td><div class="system-user-cell"><span class="system-avatar">${item.name.slice(0, 1)}</span><strong>${item.name}</strong></div></td><td>${item.account}</td><td>${item.phone}</td><td>${tag(item.role)}</td><td>${tag(item.status)}</td><td>${item.lastLogin}</td><td>${item.ip}</td><td class="action-cell"><button class="text-button" data-system-action="user-edit">编辑</button><button class="text-button" data-system-action="user-reset">重置密码</button><button class="text-button" data-system-action="user-toggle">${item.status === '启用' ? '禁用' : '启用'}</button><button class="text-button danger-link" data-system-action="user-delete">删除</button></td>`;
  renderRows(systemData, row);
  bindFilter('user-filter', systemData, (form) => { const { status, role, keyword } = form; return (item) => (!status.value || item.status === status.value) && (!role.value || item.role === role.value) && (!keyword.value.trim() || `${item.name}${item.account}${item.phone}`.includes(keyword.value.trim())); }, row);
}
function renderRoles() {
  systemData = roles;
  pageFrame('角色权限管理', '维护五种预置角色的菜单级可见权限，业务动作仍受权限矩阵和模块规则约束。', '<button class="button" data-system-action="role-create">新增角色</button>', metrics([['角色总数', roles.length, '当前预置角色'], ['启用角色', roles.filter((r) => r.status === '启用').length, '可分配给用户'], ['角色用户数', roles.reduce((sum, r) => sum + r.users, 0), '按角色汇总'], ['预置角色', roles.filter((r) => r.preset).length, 'V1 内置']]) + filterPanel('role-filter', field('关键词', 'keyword', 'text', '角色名称 / 标识', true)) + '<p class="system-note">V1 仅配置菜单级可见性；角色下仍有用户时不可删除，禁用角色后该角色下用户权限失效。</p>' + table('<thead><tr><th>角色名称</th><th>角色标识</th><th>角色说明</th><th>用户数</th><th>可用模块</th><th>最近更新</th><th>状态</th><th>操作</th></tr></thead>'));
  const row = (item) => `<td><strong>${item.name}</strong>${item.preset ? '<br><span class="muted">系统预置</span>' : ''}</td><td>${item.key}</td><td>${item.description}</td><td>${item.users}</td><td>${item.modules}</td><td>${item.updated}</td><td>${tag(item.status)}</td><td class="action-cell"><button class="text-button" data-system-action="role-edit">编辑</button><button class="text-button" data-system-action="role-permission">权限分配</button><button class="text-button" data-system-action="role-toggle">${item.status === '启用' ? '禁用' : '启用'}</button><button class="text-button danger-link" data-system-action="role-delete">删除</button></td>`;
  renderRows(systemData, row);
  bindFilter('role-filter', systemData, (form) => { const keyword = form.keyword.value.trim(); return (item) => !keyword || `${item.name}${item.key}`.includes(keyword); }, row);
}
function renderSettings() {
  pageFrame('参数配置', '维护平台基础参数、教务默认值和业务开关；保存前校验数值范围。', '<button class="button primary" data-system-action="settings-save">保存修改</button>', `<form id="settings-form"><div class="settings-layout"><section class="card settings-card"><h2>平台基础参数</h2><div class="form-grid">${field('平台名称', 'platform', 'text', '', false, settings.platform)}<label class="form-field"><span>默认时区</span><select name="timezone"><option>${settings.timezone}</option></select></label>${field('默认课时时长（分钟）', 'defaultLessonMinutes', 'number', '30-240', false, settings.defaultLessonMinutes)}${field('作业默认截止时间（小时）', 'homeworkDeadlineHours', 'number', '1-720', false, settings.homeworkDeadlineHours)}${field('最低出勤率（%）', 'attendanceThreshold', 'number', '0-100', false, settings.attendanceThreshold)}${field('最低作业提交率（%）', 'homeworkThreshold', 'number', '0-100', false, settings.homeworkThreshold)}</div><p class="ops-form-help">结业判定使用最低出勤率和最低作业提交率，待补录考勤不参与计算。</p></section><section class="card settings-card"><h2>业务开关</h2><div class="settings-switches"><label class="settings-switch"><span>启用教师端课程申报</span><span class="switch"><input type="checkbox" name="teacherApplication" ${settings.switches.teacherApplication ? 'checked' : ''}><i class="switch-track"></i></span></label><label class="settings-switch"><span>启用学员端匿名咨询</span><span class="switch"><input type="checkbox" name="anonymousConsultation" ${settings.switches.anonymousConsultation ? 'checked' : ''}><i class="switch-track"></i></span></label><label class="settings-switch"><span>启用消息失败自动重试</span><span class="switch"><input type="checkbox" name="messageRetry" ${settings.switches.messageRetry ? 'checked' : ''}><i class="switch-track"></i></span></label><label class="settings-switch"><span>允许课程顾问登记试听</span><span class="switch"><input type="checkbox" name="consultantTrial" ${settings.switches.consultantTrial ? 'checked' : ''}><i class="switch-track"></i></span></label></div></section></div><div class="form-actions"><button type="submit" class="button primary">保存修改</button></div></form>`);
  document.querySelector('#settings-form')?.addEventListener('submit', (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const lesson = Number(form.get('defaultLessonMinutes')); const deadline = Number(form.get('homeworkDeadlineHours')); const attendance = Number(form.get('attendanceThreshold')); const homeworkRate = Number(form.get('homeworkThreshold')); if (lesson < 30 || lesson > 240 || deadline < 1 || deadline > 720 || attendance < 0 || attendance > 100 || homeworkRate < 0 || homeworkRate > 100) { showToast('请检查参数范围：课时30-240分钟，截止时间1-720小时，阈值0-100%。'); return; } settings.platform = form.get('platform'); settings.defaultLessonMinutes = lesson; settings.homeworkDeadlineHours = deadline; settings.attendanceThreshold = attendance; settings.homeworkThreshold = homeworkRate; Object.keys(settings.switches).forEach((key) => { settings.switches[key] = form.get(key) === 'on'; }); showToast('参数配置已保存，变更将记录审计日志。'); });
}

function studentUserCanView() { return ['super_admin', 'academic_lead'].includes(studentUserRole); }
function studentUserCanToggle() { return studentUserRole === 'super_admin'; }
function studentUserState() { return new URLSearchParams(window.location.search).get('state') || ''; }
function studentUserRoleNotice() { return studentUserRole === 'academic_lead' ? '<p class="system-note">当前角色：教务主管。学员用户仅可查看，启用和禁用操作不展示。</p>' : ''; }
function studentUserStatePanel() {
  const state = studentUserState();
  if (state === 'loading') return '<section class="card system-state-card" aria-busy="true"><div class="system-skeleton system-skeleton-title"></div><div class="system-skeleton"></div><div class="system-skeleton"></div><p class="system-state-caption">正在加载学员用户数据…</p></section>';
  if (state === 'failure') return '<section class="card system-state-card system-state-error" role="alert"><strong>学员用户数据加载失败</strong><p>服务暂时不可用，请稍后重试；敏感账号数据未展示。</p><button class="button primary" type="button" data-student-action="retry">重试</button></section>';
  if (state === 'empty') return '<section class="card system-state-card"><strong>暂无学员用户数据</strong><p>当前筛选条件下没有可展示的家长或监护人登录账号。</p><button class="button" type="button" data-student-action="reset">清除筛选</button></section>';
  return '';
}
function studentUserFilters(id) { return filterPanel(id, field('关键词', 'keyword', 'text', '昵称 / 手机号 / 账号编号 / 学员姓名或手机号', true) + select('账号状态', 'status', ['启用', '禁用']) + select('登录方式', 'loginMethod', ['验证码登录', '密码登录', '微信授权'])); }
const studentUserStorageKey = 'training-demo-student-user-accounts-v1';
function loadStudentUserState() { try { const saved = JSON.parse(localStorage.getItem(studentUserStorageKey) || 'null'); if (Array.isArray(saved)) saved.forEach((item) => { const account = studentUserAccounts.find((entry) => entry.id === item.id); if (account) Object.assign(account, item); }); } catch (error) { console.warn('学员用户演示数据读取失败，使用初始数据。'); } }
function persistStudentUserState() { localStorage.setItem(studentUserStorageKey, JSON.stringify(studentUserAccounts)); }
function renderStudentUsersV2() {
  loadStudentUserState();
  if (!studentUserCanView()) { pageFrame('学员用户管理', '管理家长或监护人登录账号，与后台用户管理和学员档案分开。', '', '<section class="card system-state-card system-state-error" role="alert"><strong>暂无权限访问</strong><p>当前角色无权查看学员端登录账号及关联学员信息。</p><a class="button" href="/admin/index.html">返回工作台</a></section>'); return; }
  systemData = studentUserAccounts;
  const statePanel = studentUserStatePanel();
  if (['loading', 'failure', 'empty'].includes(studentUserState())) { pageFrame('学员用户管理', '管理家长或监护人登录账号，与后台用户管理和学员档案分开。', '', statePanel + studentUserRoleNotice()); return; }
  const controls = `<span class="system-role-chip">当前角色：${studentUserRole === 'super_admin' ? '超级管理员' : '教务主管'}</span>`;
  pageFrame('学员用户管理', '以家长或监护人登录账号为主对象；关联学员仅在详情页只读展示。', controls, metrics([['账号总数', studentUserAccounts.length, '家长/监护人账号'], ['启用', studentUserAccounts.filter((u) => u.status === '启用').length, '允许登录'], ['禁用', studentUserAccounts.filter((u) => u.status === '禁用').length, '禁止登录'], ['关联学员', studentUserAccounts.reduce((sum, u) => sum + u.learners.length, 0), '仅统计关联关系']]) + studentUserFilters('student-user-filter') + studentUserRoleNotice() + '<p class="system-note">列表一行对应一个登录账号，按最近登录时间倒序；手机号、学员手机号和登录 IP 均已脱敏。账号启停不改变关联学员状态或历史业务数据。</p>' + table('<thead><tr><th>账号</th><th>登录方式</th><th>关联学员数</th><th>账号状态</th><th>最近登录</th><th>注册时间</th><th>操作</th></tr></thead>'));
  const row = (item) => `<td><div class="system-user-cell"><span class="system-avatar">${escapeHtml(item.avatar)}</span><div><strong>${escapeHtml(item.name)}</strong><small class="muted">${escapeHtml(item.accountNo)} · ${escapeHtml(item.phone)}</small></div></div></td><td>${escapeHtml(item.loginMethod)}</td><td>${item.learners.length}</td><td>${tag(item.status)}</td><td>${escapeHtml(item.lastLoginAt)}</td><td>${escapeHtml(item.registeredAt)}</td><td class="action-cell"><a class="text-button" href="/admin/pages/system/student-users.html?accountId=${encodeURIComponent(item.id)}&role=${encodeURIComponent(studentUserRole)}">查看详情</a>${studentUserCanToggle() ? `<button class="text-button" type="button" data-system-action="student-user-toggle">${item.status === '启用' ? '禁用' : '启用'}</button>` : ''}</td>`;
  renderRows(studentUserAccounts, row);
  bindFilter('student-user-filter', studentUserAccounts, (form) => { const keyword = form.keyword.value.trim(); return (item) => (!keyword || `${item.name}${item.phone}${item.accountNo}${item.learners.map((learner) => `${learner.name}${learner.phone}`).join('')}`.includes(keyword)) && (!form.status.value || item.status === form.status.value) && (!form.loginMethod.value || item.loginMethod === form.loginMethod.value); }, row);
}
function renderStudentUserDetail(account) {
  if (!studentUserCanView()) { renderStudentUsers(); return; }
  systemData = studentUserAccounts;
  if (!account) { pageFrame('学员用户详情', '未找到对应的学员端登录账号。', '', '<section class="card system-state-card system-state-error"><strong>账号不存在或已被移除</strong><p>请返回学员用户列表重新选择。</p><a class="button" href="/admin/pages/system/student-users.html">返回列表</a></section>'); return; }
  const action = studentUserCanToggle() ? `<button class="button ${account.status === '启用' ? 'danger-button' : 'primary'}" type="button" data-system-action="student-user-toggle">${account.status === '启用' ? '禁用账号' : '启用账号'}</button>` : '';
  pageFrame('学员用户详情', '仅查看账号信息、关联学员和状态变更记录；关联学员不在此编辑。', `<a class="button" href="/admin/pages/system/student-users.html?role=${encodeURIComponent(studentUserRole)}">返回列表</a>${action}`, `<div class="profile-banner card"><div class="display-profile"><span class="avatar-placeholder">${escapeHtml(account.avatar)}</span><div><span class="tag ${account.status === '启用' ? 'green' : 'gray'}">${escapeHtml(account.status)}</span><h2>${escapeHtml(account.name)}</h2><p>${escapeHtml(account.accountNo)} · ${escapeHtml(account.phone)}</p></div></div><div class="profile-summary"><span>关联学员</span><strong>${account.learners.length} 人</strong></div></div><div class="detail-grid"><section class="card"><h2>账号信息</h2><dl class="info-grid"><div><dt>账号编号</dt><dd>${escapeHtml(account.accountNo)}</dd></div><div><dt>账号昵称</dt><dd>${escapeHtml(account.name)}</dd></div><div><dt>登录手机号</dt><dd>${escapeHtml(account.phone)}</dd></div><div><dt>登录方式</dt><dd>${escapeHtml(account.loginMethod)}</dd></div><div><dt>账号状态</dt><dd>${tag(account.status)}</dd></div><div><dt>注册时间</dt><dd>${escapeHtml(account.registeredAt)}</dd></div><div><dt>首次登录时间</dt><dd>${escapeHtml(account.firstLoginAt)}</dd></div><div><dt>最近登录时间</dt><dd>${escapeHtml(account.lastLoginAt)}</dd></div><div><dt>最近登录 IP</dt><dd>${escapeHtml(account.lastLoginIp)}</dd></div><div><dt>协议同意时间</dt><dd>${escapeHtml(account.consentAt)}</dd></div><div><dt>禁用原因</dt><dd>${escapeHtml(account.disabledReason || '—')}</dd></div><div><dt>最近状态变更</dt><dd>${escapeHtml(account.stateChangedAt)} · ${escapeHtml(account.stateChangedBy)}</dd></div></dl></section><section class="card"><div class="section-title-row"><div><h2>关联学员</h2><p>只读展示，不支持新增、解绑、切换或编辑。</p></div><span class="tag gray">${account.learners.length} 人</span></div><div class="table-wrap student-subtable-wrap"><table class="student-subtable"><thead><tr><th>学员</th><th>关系</th><th>性别</th><th>出生日期</th><th>手机号</th><th>学员状态</th><th>当前学员</th><th>关联时间</th><th>在读班级</th><th>已报名课程</th></tr></thead><tbody>${account.learners.map((learner) => `<tr><td><strong>${escapeHtml(learner.name)}</strong></td><td>${escapeHtml(learner.relation)}</td><td>${escapeHtml(learner.gender)}</td><td>${escapeHtml(learner.birthday)}</td><td>${escapeHtml(learner.phone)}</td><td>${escapeHtml(learner.status)}</td><td>${learner.current ? '<span class="tag brand">当前学员</span>' : '—'}</td><td>${escapeHtml(learner.linkedAt)}</td><td>${learner.classes}</td><td>${learner.courses}</td></tr>`).join('')}</tbody></table></div></section><section class="card detail-grid-wide"><div class="section-title-row"><div><h2>账号操作记录</h2><p>仅记录实际状态变更；重复操作不新增业务结果。</p></div><span class="tag gray">审计记录</span></div><div class="table-wrap student-subtable-wrap"><table class="student-audit-table"><thead><tr><th>操作</th><th>操作前状态</th><th>操作后状态</th><th>操作人</th><th>操作时间</th><th>操作原因</th></tr></thead><tbody>${account.audits.map((audit) => `<tr><td>${escapeHtml(audit.action)}</td><td>${escapeHtml(audit.before)}</td><td>${escapeHtml(audit.after)}</td><td>${escapeHtml(audit.operator)}</td><td>${escapeHtml(audit.at)}</td><td>${escapeHtml(audit.reason)}</td></tr>`).join('')}</tbody></table></div></section></div>`);
}
function openStudentUserToggle(account) {
  const disabling = account.status === '启用';
  const body = `<form id="student-user-toggle-form" class="system-dialog-grid"><p class="wide">将${disabling ? '禁用' : '启用'}账号“${escapeHtml(account.name)}”（${escapeHtml(account.accountNo)}）。${disabling ? '禁用后当前有效会话立即失效，关联学员和历史业务数据保留。' : '启用后账号可重新登录，不自动恢复旧会话。'}</p>${disabling ? '<label class="form-field wide"><span>禁用原因 *</span><textarea name="reason" aria-required="true" placeholder="请输入禁用原因"></textarea></label>' : ''}<p class="form-error wide" data-student-user-error hidden></p></form>`;
  const dialog = openDialog(`${disabling ? '禁用' : '启用'}学员用户`, disabling ? '请填写原因后确认，操作将写入审计记录。' : '请确认账号状态变更，操作将写入审计记录。', body, `<button type="button" class="button" data-dialog-close>取消</button><button type="submit" form="student-user-toggle-form" class="button primary">确认${disabling ? '禁用' : '启用'}</button>`);
  dialog.querySelector('#student-user-toggle-form')?.addEventListener('submit', (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const error = dialog.querySelector('[data-student-user-error]'); const reason = String(form.get('reason') || '').trim(); if (disabling && !reason) { error.textContent = '禁用原因不能为空。'; error.removeAttribute('hidden'); return; } if (new URLSearchParams(window.location.search).get('action') === 'failure') { error.textContent = '提交失败，请稍后重试；账号状态未改变。'; error.removeAttribute('hidden'); return; } const before = account.status; account.status = disabling ? '禁用' : '启用'; account.disabledReason = disabling ? reason : ''; account.stateChangedAt = '2026-09-11 18:00'; account.stateChangedBy = studentUserRole === 'super_admin' ? '平台管理员' : '李教务'; account.audits.unshift({ action: account.status, before, after: account.status, operator: account.stateChangedBy, at: account.stateChangedAt, reason: disabling ? reason : '恢复账号登录' }); persistStudentUserState(); closeDialog(); showToast(`账号已${account.status}，操作已记录审计日志。`); const id = new URLSearchParams(window.location.search).get('accountId'); if (id) renderStudentUserDetail(account); else renderStudentUsers(); });
}

function openUserForm(row) { return openSimpleForm(row ? '编辑后台用户' : '新增后台用户', '账号、角色和状态变更会影响后台登录与菜单可见性。', field('用户姓名', 'name', 'text', '请输入姓名') + field('登录账号', 'account', 'text', '请输入唯一账号') + field('手机号', 'phone', 'text', '请输入手机号') + select('所属角色', 'role', ['超级管理员', '教研主管', '教务主管', '课程顾问', '财务'], false, false) + select('用户状态', 'status', ['启用', '禁用'], false, false) + field('初始密码', 'password', 'password', row ? '留空则不修改' : '请输入初始密码'), (form) => { if (!String(form.get('name') || '').trim() || !String(form.get('account') || '').trim() || !String(form.get('role') || '').trim()) { showToast('姓名、登录账号和所属角色为必填项。'); return; } closeDialog(); showToast(row ? '用户信息已保存。' : '用户已创建，初始密码将通过安全渠道通知。'); }); }
function openSimpleForm(title, subtitle, body, onSubmit) { const dialog = openDialog(title, subtitle, `<form id="system-form" class="system-dialog-grid">${body}</form>`, '<button type="button" class="button" data-dialog-close>取消</button><button type="submit" form="system-form" class="button primary">确认</button>'); dialog.querySelector('#system-form')?.addEventListener('submit', (event) => { event.preventDefault(); onSubmit(new FormData(event.currentTarget), dialog); }); return dialog; }
function handleAction(action, row) {
  if (systemPage === 'student-users' && !systemData) systemData = studentUserAccounts;
  if ((action === 'student-user-toggle' || action === 'student-toggle') && row && studentUserCanToggle()) return openStudentUserToggle(row);
  if (action === 'student-detail') return openStudentDetail(row);
  if (action === 'student-toggle') { const target = row.status === '启用' ? '禁用' : '启用'; const reason = target === '禁用' ? prompt('请输入禁用原因') : ''; if (target === '禁用' && !reason?.trim()) { showToast('禁用原因必填。'); return; } if (!confirm(`确认${target}账号“${row.name}”？`)) return; row.status = target; row.disabledReason = reason || ''; row.audits.push({ action: target, before: target === '禁用' ? '启用' : '禁用', after: target, operator: '超级管理员', time: new Date().toLocaleString('zh-CN'), reason: reason || '重新启用' }); renderStudentUsers(); showToast(`账号已${target}，状态变更已记录审计。`); return; }
  if (action === 'user-create' || action === 'user-edit') return openUserForm(row);
  if (action === 'user-reset') { const dialog = openDialog('确认重置密码', '新密码将通过安全渠道发送至用户手机。', `<p>确认重置“${escapeHtml(row.name)}”的登录密码？原密码将立即失效。</p>`, '<button type="button" class="button" data-dialog-close>取消</button><button type="button" class="button primary" data-confirm-action="user-reset">确认重置</button>'); dialog.dataset.rowId = row.id; return dialog; }
  if (action === 'user-toggle') { const target = row.status === '启用' ? '禁用' : '启用'; const dialog = openDialog(`确认${target}用户`, target === '禁用' ? '禁用后用户将无法登录，历史业务数据保留。' : '启用后用户可以重新登录后台。', `<p>确认${target}“${escapeHtml(row.name)}”？</p>`, `<button type="button" class="button" data-dialog-close>取消</button><button type="button" class="button primary" data-confirm-action="user-toggle">确认${target}</button>`); dialog.dataset.rowId = row.id; return dialog; }
  if (action === 'user-delete') { const dialog = openDialog('确认删除用户', '此操作不可恢复；业务数据和审计记录不会被删除。', `<p class="system-danger">确认删除“${escapeHtml(row.name)}”账号？建议优先禁用账号，避免误删仍被业务引用的用户。</p>`, '<button type="button" class="button" data-dialog-close>取消</button><button type="button" class="button danger-button" data-confirm-action="user-delete">确认删除</button>'); dialog.dataset.rowId = row.id; return dialog; }
  if (action === 'role-create' || action === 'role-edit') return openSimpleForm(action === 'role-create' ? '新增角色' : '编辑角色', '角色用于菜单级可见性控制，业务动作仍按权限矩阵执行。', field('角色名称', 'name', 'text', '请输入角色名称') + field('角色标识', 'key', 'text', '请输入唯一标识') + field('角色说明', 'description', 'text', '请输入职责说明', true) + select('角色状态', 'status', ['启用', '禁用'], false, false), () => { closeDialog(); showToast('角色信息已保存。'); });
  if (action === 'role-permission') return openPermissionDialog(row);
  if (action === 'role-toggle') { const target = row.status === '启用' ? '禁用' : '启用'; const dialog = openDialog(`确认${target}角色`, target === '禁用' ? '禁用后该角色下用户权限立即失效。' : '启用后该角色可继续分配给后台用户。', `<p>确认${target}角色“${escapeHtml(row.name)}”？</p>`, `<button type="button" class="button" data-dialog-close>取消</button><button type="button" class="button primary" data-confirm-action="role-toggle">确认${target}</button>`); dialog.dataset.rowId = row.id; return dialog; }
  if (action === 'role-delete') { if (row.users > 0) return openDialog('无法删除角色', '删除前需要转移该角色下的用户。', `<p class="system-danger">该角色下仍有 ${row.users} 名用户，请先转移用户后再操作。</p>`); const dialog = openDialog('确认删除角色', '删除后角色记录不可恢复。', `<p>确认删除角色“${escapeHtml(row.name)}”？</p>`, '<button type="button" class="button" data-dialog-close>取消</button><button type="button" class="button danger-button" data-confirm-action="role-delete">确认删除</button>'); dialog.dataset.rowId = row.id; return dialog; }
}
function openPermissionDialog(row) { const body = `<div class="permission-groups">${permissionGroups.map(([group, items]) => `<section class="permission-group"><h3>${group}</h3><div class="permission-items">${items.map((item, index) => `<label class="permission-item"><input type="checkbox" ${row.name === '超级管理员' || (row.name === '教务主管' && ['查看', '排班与执行', '结业复核', '维护', '出入库', '台账导出'].includes(item)) ? 'checked' : ''} data-permission="${escapeHtml(group)}-${escapeHtml(item)}"><span>${item}</span></label>`).join('')}</div></section>`).join('')}</div>`; return openSimpleForm('权限分配', `为“${row.name}”配置菜单级可见权限。`, body, () => { closeDialog(); showToast('角色权限已保存，变更将记录审计日志。'); }); }

document.addEventListener('submit', (event) => { if (event.target.id === 'student-user-toggle-form') setTimeout(persistStudentUserState, 0); }, true);
document.addEventListener('click', (event) => {
  if (!systemRoot) return;
  if (event.target.closest('[data-dialog-close]')) { closeDialog(); return; }
  const confirm = event.target.closest('[data-confirm-action]');
  if (confirm) { const dialog = confirm.closest('dialog'); const action = confirm.dataset.confirmAction; const row = systemData.find((item) => item.id === dialog?.dataset.rowId); if (action === 'user-reset' && row) { closeDialog(); showToast('密码已重置，通知已发送至用户安全联系方式。'); } if (action === 'user-toggle' && row) { row.status = row.status === '启用' ? '禁用' : '启用'; closeDialog(); renderUsers(); showToast(`用户已${row.status}。`); } if (action === 'user-delete' && row) { users.splice(users.findIndex((item) => item.id === row.id), 1); closeDialog(); renderUsers(); showToast('用户已删除。'); } if (action === 'role-toggle' && row) { row.status = row.status === '启用' ? '禁用' : '启用'; closeDialog(); renderRoles(); showToast(`角色已${row.status}。`); } if (action === 'role-delete' && row) { roles.splice(roles.findIndex((item) => item.id === row.id), 1); closeDialog(); renderRoles(); showToast('角色已删除。'); } return; }
  const button = event.target.closest('[data-system-action]'); if (!button) return; const action = button.dataset.systemAction; const rowElement = button.closest('tr[data-row-id]'); const row = rowElement ? systemData.find((item) => item.id === rowElement.dataset.rowId) : null; handleAction(action, row);
});
document.addEventListener('click', (event) => {
  if (event.target.closest('[data-student-action="retry"]')) { window.location.href = `${window.location.pathname}?role=${encodeURIComponent(studentUserRole)}`; return; }
  if (event.target.closest('[data-student-action="reset"]')) { window.location.href = `${window.location.pathname}?role=${encodeURIComponent(studentUserRole)}`; return; }
  const button = event.target.closest('[data-system-action="student-user-toggle"]');
  if (!button || !studentUserCanToggle() || button.closest('tr[data-row-id]')) return;
  const id = button.closest('tr[data-row-id]')?.dataset.rowId || new URLSearchParams(window.location.search).get('accountId');
  const account = studentUserAccounts.find((item) => item.id === id);
  if (account) openStudentUserToggle(account);
});
if (systemPage === 'users') renderUsers();
if (systemPage === 'student-users') { loadStudentUserState(); const accountId = new URLSearchParams(window.location.search).get('accountId'); if (accountId) renderStudentUserDetail(studentUserAccounts.find((item) => item.id === accountId)); else renderStudentUsersV2(); }
if (systemPage === 'roles') renderRoles();
if (systemPage === 'settings') renderSettings();
