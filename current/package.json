{
  "name": "training-platform-prototype",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check:page-spec": "node scripts/check-page-spec.mjs",
    "build:prd-fields": "node scripts/build-prd-fields.mjs",
    "check:prd-fields": "node scripts/build-prd-fields.mjs --check",
    "build:prd-states": "node scripts/build-prd-states.mjs",
    "check:prd-states": "node scripts/build-prd-states.mjs --check",
    "check:changes": "node scripts/check-change-coverage.mjs",
    "check:spec": "npm run check:page-spec && npm run check:prd-fields && npm run check:prd-states && npm run check:changes && npm run check:params && npm run check:course-hours",
    "audit:alignment": "node scripts/audit-field-alignment.mjs",
    "check:params": "node scripts/check-parameter-consistency.mjs",
    "check:course-hours": "node scripts/check-course-hours.mjs"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
